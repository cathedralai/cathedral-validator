# Validate Cathedral

Cathedral Validator checks Cathedral's signed weight vector and submits weights to Bittensor SN39.

Public validator operation is not self-service during live testing.

Do not run a chain-writing validator unless you are an authorized Cathedral operator using a reviewed release.

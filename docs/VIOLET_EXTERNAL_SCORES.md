# Retired external-score integration

This page previously documented an external-score publisher mode for the
signed weight-feed validator. That mode is not part of the recurring direct
validator and its command is not installed.

Use the [current validator guide](../README.md). The former integration remains
in Git history for design review only and is not current operating authority.

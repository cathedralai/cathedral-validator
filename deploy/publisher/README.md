# Retired SN39 weight publisher deployment

This directory is not a supported validator installation path. Its files are
retained only for historical release reconstruction.

The recurring direct validator derives weights locally and submits them without
a signed weight publisher or relay. Use the
[current validator guide](../../README.md). Do not install or start the
historical publisher services for the direct validator.
